// reset the form
$('#new_item').trigger("reset");

$("#donor-items").append('<%= escape_javascript(render 'item_row', item: @item, donor: @donor )%>');

// if submit button was not xxxxx then close the form
